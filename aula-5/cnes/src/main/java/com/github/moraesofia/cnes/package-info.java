/**
 * Este programa recebe via linha de comando dois argumentos: (1) Url contendo
 * um arquivo .zip que será baixado, descomprimido, o arquivo .csv de interesse
 * será processado (extração das quatro colunas de interesse) e, para cada
 * linha, um objeto JSON será depositado em um vetor (JSON) a ser persistido.
 * (2) o arquivo JSON no qual será depositado o vetor de objetos.
 */
package com.github.moraesofia.cnes;
